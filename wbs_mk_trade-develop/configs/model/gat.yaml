# ── GAT encoder config ────────────────────────────────────
encoder:
  name: "GAT"
  conv: "GATConv"
  hidden_channels: 128
  num_layers: 2
  heads: 4
  dropout: 0.3
  heterogeneous: true

decoder:
  name: "dot_product"

variational: false
